export enum Tab {
	SIGN_IN = "signIn",
	SIGN_UP = "signUp",
}

export type FormikProps = {
	email: string;
	password: string;
	regEmail: string;
	regPassword: string;
	bussinessName: string;
	confirmPassword: string;
};
